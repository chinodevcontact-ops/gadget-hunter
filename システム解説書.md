# 📚 GADGET HUNTER システム完全解説書

> **対象読者**: 日本電子専門学校の学生、Webアプリケーション初学者
> 
> **最終更新**: 2026年3月2日
>
> この資料では、GADGET HUNTERの仕組みを**ゼロから丁寧に**解説します。状況に応じて随時更新されます。

---

## 📖 目次

1. [システム概要](#1-システム概要)
2. [全体アーキテクチャ](#2-全体アーキテクチャ)
3. [データフロー（データの流れ）](#3-データフローデータの流れ)
4. [各コンポーネント詳細解説](#4-各コンポーネント詳細解説)
5. [セキュリティ対策](#5-セキュリティ対策)
6. [コード詳細解説](#6-コード詳細解説)
7. [デプロイとCI/CD](#7-デプロイとcicd)
8. [トラブルシューティング](#8-トラブルシューティング)
9. [用語集](#9-用語集)

---

## 1. システム概要

### 🎯 GADGET HUNTERとは？

**GADGET HUNTER**は、海外のテックニュースやリーク情報を自動で収集・翻訳・要約し、日本語・英語で配信する**完全自動運営のWebメディア**です。

### ✨ 主な機能

| 機能 | 説明 |
|------|------|
| 🔍 **自動ニュース収集** | 10以上のRSSフィードから最新情報を2時間ごとに収集 |
| 🤖 **AI自動記事生成** | Google Gemini APIで日本語・英語の記事を自動作成 |
| 📊 **信頼度スコア判定** | リーク情報の信頼性を0-100%で自動評価 |
| 🔄 **重複検知・統合** | 同じニュースを複数ソースから検出し、情報を統合 |
| 🐦 **X(Twitter)自動投稿** | 新着記事を自動で2段階投稿（本文→リプライでURL） |
| 🌐 **多言語対応** | 日本語・英語のリアルタイム切り替え |
| 🔒 **エンタープライズセキュリティ** | XSS対策、CSRF対策、CSP実装 |

### 🎓 学習ポイント

このプロジェクトを学ぶことで、以下の技術を習得できます：

- **バックエンド開発**: Google Apps Script (TypeScript)
- **フロントエンド開発**: HTML/CSS/JavaScript (Vanilla JS)
- **API連携**: REST API、OAuth認証、RSS解析
- **AI活用**: プロンプトエンジニアリング、生成AI
- **インフラ**: Vercel、GitHub Actions、Google Drive
- **セキュリティ**: XSS対策、CSP、入力検証
- **CI/CD**: 自動デプロイ、自動テスト

---

## 2. 全体アーキテクチャ

### 🏗️ システム構成図

```
┌─────────────────────────────────────────────────────────────┐
│                    GADGET HUNTER システム                      │
└─────────────────────────────────────────────────────────────┘

┌──────────────────────┐
│  ① データソース       │
│  (RSS Feeds)         │
│  ・Wccftech          │
│  ・TechPowerUp       │
│  ・VideoCardz        │
│  ・nitter (Twitter)  │
│  ・MacRumors         │
│  └─ 他10サイト       │
└──────────────────────┘
         │
         │ HTTP Request (2時間ごと)
         ↓
┌──────────────────────────────────────────────┐
│  ② Google Apps Script (バックエンド)           │
│  ─────────────────────────────────────        │
│  📝 backend/Code.ts (約1250行)                │
│  ・fetchAndSummarizeToSheet()               │
│  ・parseRSSRegex()                          │
│  ・callGeminiAPI()                          │
│  ・calculateSimilarity()                    │
│  ・checkAndTweetNewArticles()               │
│  └─ 処理内容:                                 │
│     1. RSS解析                               │
│     2. 重複チェック（Jaccard類似度）          │
│     3. AI記事生成（Gemini API）              │
│     4. Google Sheetsに保存                   │
│     5. 古い記事のクリーンアップ（日付・行数制限） │
│     6. JSONファイル生成                       │
│     7. Google Driveに保存                    │
│     8. X(Twitter)投稿（人間らしい待機時間）    │
└──────────────────────────────────────────────┘
         │
         │ JSON Output
         ↓
┌──────────────────────────────────────────────┐
│  ③ Google Drive (データストレージ)             │
│  ─────────────────────────────────────        │
│  📁 frontend/data/news.json（File ID固定）     │
│  ・最新300行まで保持（CLEANUP_MAX_ROWS）       │
│  ・30日以上古い記事は自動削除（CLEANUP_DAYS）  │
│  ・日本語・英語の両言語対応・リーク信頼度スコア │
└──────────────────────────────────────────────┘
         │
         │ GitHub Actions (1時間ごと)
         ↓
┌──────────────────────────────────────────────┐
│  ④ GitHub Actions (自動更新システム)           │
│  ─────────────────────────────────────        │
│  📝 .github/workflows/update-news.yml        │
│  └─ 処理内容:                                 │
│     1. Google DriveからJSONダウンロード       │
│     2. 🛡️ JSON整合性検証 (jq) ← 壊れたデータのコミット防止 │
│     3. 変更検知 (git diff)                   │
│     4. GitHubリポジトリにコミット             │
│     5. Vercelの自動デプロイをトリガー         │
└──────────────────────────────────────────────┘
         │
         │ Git Push
         ↓
┌──────────────────────────────────────────────┐
│  ⑤ GitHub Repository (バージョン管理)          │
│  ─────────────────────────────────────        │
│  📂 chinodevcontact-ops/gadget-hunter        │
│  ・ソースコード管理                            │
│  ・バージョン履歴                              │
│  ・Issue・PR管理                              │
└──────────────────────────────────────────────┘
         │
         │ Webhook
         ↓
┌──────────────────────────────────────────────┐
│  ⑥ Vercel (ホスティング・CDN)                  │
│  ─────────────────────────────────────        │
│  🌐 https://gadget-hunter-xi.vercel.app/     │
│  ・静的サイトホスティング                      │
│  ・グローバルCDN配信                          │
│  ・自動HTTPS化                                │
│  ・セキュリティヘッダー設定                    │
└──────────────────────────────────────────────┘
         │
         │ HTTPS
         ↓
┌──────────────────────────────────────────────┐
│  ⑦ フロントエンド (ユーザーインターフェース)    │
│  ─────────────────────────────────────        │
│  📝 frontend/index.html (2117行)             │
│  ・記事表示                                   │
│  ・検索・フィルター機能                        │
│  ・言語切り替え                                │
│  ・レスポンシブデザイン                        │
│  ・モーダル表示                                │
└──────────────────────────────────────────────┘
         │
         │ User Interaction
         ↓
     👤 ユーザー
```

### 🔄 技術スタック

| レイヤー | 技術 | 役割 |
|---------|------|------|
| **バックエンド** | Google Apps Script (TypeScript) | RSSパース、AI処理、データ保存 |
| **AI** | Google Gemini 2.5 Flash | 記事の翻訳・要約・生成（10 RPM、10〜15秒間隔）|
| **データベース** | Google Sheets + Google Drive | 記事データの永続化 |
| **CI/CD** | GitHub Actions | 自動更新・デプロイ |
| **ホスティング** | Vercel | 静的サイト配信 |
| **フロントエンド** | HTML/CSS/JavaScript | UI・UX |
| **セキュリティ** | CSP, XSS対策, CORS | 脆弱性対策 |
| **SNS連携** | X (Twitter) API v2 | 自動投稿 |

---

## 3. データフロー（データの流れ）

### 📊 メインフロー：ニュース収集→配信

```
START: GASタイマートリガー (2時間ごと)
  │
  ├──► 1️⃣ RSS取得フェーズ
  │    └─ for each サイト (10サイト):
  │        ├─ URLFetchApp.fetch(rss_url)
  │        ├─ parseRSSRegex() → { title, link, desc }[]
  │        └─ 最大10件まで取得
  │
  ├──► 2️⃣ 重複チェックフェーズ
  │    └─ for each 記事:
  │        ├─ getRecentTitles() → 過去24時間の記事取得
  │        ├─ findDuplicate() → Jaccard類似度計算
  │        │   └─ 類似度 ≥ 70% なら統合処理
  │        └─ 統合時: 既存記事を更新 (信頼度+15)
  │
  ├──► 3️⃣ AI生成フェーズ
  │    └─ for each 新規記事:
  │        ├─ callGeminiAPI()
  │        │   ├─ プロンプト生成 (日本語・英語)
  │        │   ├─ Gemini API呼び出し
  │        │   └─ JSONレスポンス解析
  │        ├─ calculateLeakScore() → 信頼度計算
  │        └─ Google Sheetsに追加
  │
  ├──► 4️⃣ リトライフェーズ
  │    └─ retryFailedArticles()
  │        └─ 「【翻訳失敗】」記事を再生成
  │
  ├──► 5️⃣ データ保存フェーズ
  │    └─ saveJsonToDrive()
  │        ├─ Google Sheetsからデータ読み込み
  │        ├─ JSON形式に変換
  │        └─ Google Driveに保存 (news.json)
  │
  └──► 6️⃣ 自動投稿フェーズ
       └─ checkAndTweetNewArticles()
           ├─ 未投稿記事を1件取得 (優先度順)
           ├─ postMainText() → テキスト投稿
           ├─ 5秒待機
           └─ postReplyUrl() → リプライでURL投稿

END: 次回実行まで待機
```

### 🔄 デプロイフロー：GAS → GitHub → Vercel

```
START: GAS実行完了
  │
  ├──► 1️⃣ Google Drive更新
  │    └─ news.json が最新データで上書き
  │
  ├──► 2️⃣ GitHub Actions起動 (1時間ごと)
  │    └─ update-news.yml 実行:
  │        ├─ curl で Google Drive から JSON取得
  │        ├─ 🛡️ jq で JSON 整合性検証（失敗時はコミットしない）
  │        ├─ git diff でファイル差分チェック
  │        ├─ 変更あり → git commit & push
  │        └─ 変更なし → スキップ
  │
  ├──► 3️⃣ GitHub Webhook発火
  │    └─ Vercelに通知
  │
  ├──► 4️⃣ Vercel自動ビルド
  │    ├─ npm run build（記事ごとのOGP用HTML生成: frontend/articles/{slug}/）
  │    ├─ frontend/ ディレクトリをビルド
  │    ├─ セキュリティヘッダー適用
  │    ├─ CDNにキャッシュ配布
  │    └─ デプロイ完了 (約30秒)
  │
  └──► 5️⃣ ユーザーアクセス
       └─ 最新ニュースが即座に表示

END: リアルタイム配信完了
```

---

## 4. 各コンポーネント詳細解説

### 4.1 バックエンド (Google Apps Script)

#### 📝 ファイル: `backend/Code.ts`

**役割**: ニュース収集、AI処理、データ保存、X投稿を一括管理

#### 主要関数の解説

##### 🔹 `fetchAndSummarizeToSheet()`

**目的**: メイン処理関数（全体の司令塔）

**処理フロー**:
```typescript
function fetchAndSummarizeToSheet() {
  // 1. Google Sheets取得
  const ss = SpreadsheetApp.openById(getConfig('SPREADSHEET_ID'));
  const sheet = ss.getSheets()[0];
  
  // 2. 為替レート取得 (USD→JPY)
  const currentRate = getUsdJpyRate(); // 例: 150円
  
  // 3. 過去記事取得（重複チェック用）
  const recentTitles = getRecentTitles(sheet); // 過去24時間
  
  // 4. 各サイトからRSS取得
  for (const site of TARGETS) {
    const items = parseRSSRegex(fetchRss(site.url));
    
    // 5. 各記事を処理
    for (const item of items) {
      // 重複チェック
      const duplicate = findDuplicate(item.title, recentTitles);
      if (duplicate) {
        // 情報統合（信頼度+15）
        mergeArticle(duplicate, item);
        continue;
      }
      
      // AI生成
      const article = callGeminiAPI(item.title, item.desc);
      if (!article) continue; // ノイズ判定
      
      // 保存
      sheet.appendRow([...article]);
    }
  }
  
  // 6. 後処理
  retryFailedArticles(30);  // 直近30行の失敗記事をリトライ
  cleanupAndSave(sheet);    // 古い記事削除 + JSONエクスポート
}
```

**重要ポイント**:
- `apiCallCount`: Gemini 2.5 Flashの10 RPM制限に対応し最大**20回/実行**に制限
- `humanLikeSleep(10000, 15000)`: 各API呼び出し後に**10〜15秒待機**
- `rateLimitHit`フラグ: 429エラー検知で**即座にループ中断** → `retryFailedArticles`スキップ → `cleanupAndSave`のみ実行（今の状態を保存して終了）
- **429発生時**: 次の1時間トリガーで続きから処理される

---

##### 🔹 `parseRSSRegex(xmlText)`

**目的**: XMLパーサーを使わず正規表現でRSSを解析（軽量・高速）

**注意**: コード内に `TODO: Refactor to XmlService when RSS format changes` を記載。フィード仕様差やReDoS対策が必要になったら XmlService への移行を検討する。

**処理例**:
```typescript
function parseRSSRegex(xmlText) {
  // <item> または <entry> タグを抽出
  const itemMatches = xmlText.match(/<(item|entry)>[\s\S]*?<\/\1>/gi);
  
  for (const itemStr of itemMatches) {
    // タイトル抽出
    const titleMatch = itemStr.match(/<title[^>]*>([\s\S]*?)<\/title>/i);
    const title = titleMatch ? decodeHTMLEntities(titleMatch[1]) : "No Title";
    
    // リンク抽出
    const linkMatch = itemStr.match(/<link>([\s\S]*?)<\/link>/i);
    const link = linkMatch ? linkMatch[1].trim() : "";
    
    // 説明文抽出
    const descMatch = itemStr.match(/<(description|content)[^>]*>([\s\S]*?)<\/\1>/i);
    let desc = descMatch ? descMatch[2] : "";
    
    // HTMLタグ削除
    desc = desc.replace(/<[^>]*>/g, '').substring(0, 3000);
    
    items.push({ title, link, desc });
  }
  return items;
}
```

**なぜ正規表現？**:
- GASの標準XMLパーサーは遅い
- シンプルなRSS構造なら正規表現で十分
- パフォーマンスが約3倍向上

**将来の改善**: RSS仕様差やReDoSが問題になったら、GAS標準の `XmlService` へのリファクタを検討（解説書・シニアSEレビュー回答を参照）

---

##### 🔹 `calculateSimilarity(text1, text2)`

**目的**: 2つの記事タイトルの類似度を計算（Jaccard係数）

**アルゴリズム**:
```typescript
function calculateSimilarity(text1, text2) {
  // 1. テキスト正規化
  const normalize = (text) => {
    return text.toLowerCase()
      .replace(/[^\w\s]/g, ' ')  // 記号削除
      .split(/\s+/)               // 単語分割
      .filter(w => w.length > 2); // 短い単語除外
  };
  
  // 2. 単語セット作成
  const words1 = new Set(normalize(text1));
  const words2 = new Set(normalize(text2));
  
  // 3. Jaccard係数計算
  // J(A, B) = |A ∩ B| / |A ∪ B|
  const intersection = [...words1].filter(w => words2.has(w)).length;
  const union = words1.size + words2.size - intersection;
  
  return union > 0 ? intersection / union : 0;
}
```

**使用例**:
```javascript
// タイトル1: "RTX 5090 Leaked Specs"
// タイトル2: "NVIDIA RTX 5090 Specifications Leak"
// → 類似度: 0.75 (75%)

// タイトル1: "AMD Ryzen 9 9950X"
// タイトル2: "Intel Core i9-14900K"
// → 類似度: 0.10 (10%)
```

**閾値**: 70%以上で重複判定

---

##### 🔹 `callGeminiAPI()`

**目的**: Google Gemini APIで記事を自動生成

**プロンプト戦略（v4.0 2ペルソナ構成）**:

記事本文とコメントで**別々のペルソナ**を使い分ける：

| フィールド | ペルソナ | 文体 |
|---|---|---|
| `title_jp/en` | プロライター | キャッチーだが事実ベース |
| `summary_points` | プロライター | 感嘆符・スラング禁止 |
| `body_text` | プロライター（Tom's Hardware相当） | 「僕」「私」禁止、報道文体 |
| `review_text` | 19歳の自作er学生 | スラング全開・本音・予算感・AMD偏愛 |
| `review_text_en` | 同上 | QRT用短いリアクション（最大100文字） |

**レスポンス処理**:
```typescript
const response = UrlFetchApp.fetch(apiUrl, {
  method: "post",
  contentType: "application/json",
  payload: JSON.stringify({ contents: [{ parts: [{ text: prompt }] }] })
});

const rawText = JSON.parse(response.getContentText()).candidates[0].content.parts[0].text;

// JSONクリーニング（```json ```を削除）
let cleanJson = rawText.replace(/```json/g, "").replace(/```/g, "").trim();

// nullチェック（ノイズ判定）
if (cleanJson === "null") return null;

// JSON解析
return JSON.parse(cleanJson);
```

**ノイズフィルタリング**:
- AIが「null」を返す = スパム・無関係な情報
- キーワード必須: `RTX|GTX|Ryzen|Intel|AMD|...`
- 最低文字数: 20文字以上

---

##### 🔹 `checkAndTweetNewArticles()`

**目的**: 未投稿記事を自動でX(Twitter)に投稿

**2段階投稿戦略**:
```typescript
function checkAndTweetNewArticles() {
  // 1. 未投稿記事を取得
  const untweetedArticles = getUntweetedArticles();
  if (untweetedArticles.length === 0) return;
  
  // 2. 優先度判定（キーワードマッチ）
  const priorityArticle = findPriorityArticle(untweetedArticles);
  
  // 3. Stage 1: テキスト投稿
  const mainText = `
🚨【CONFIDENTIAL】

${article.title}

${article.summary.split('\n')[0]}...

#GadgetHunter
  `;
  const mainTweetId = postMainText(mainText);
  
  // 4. Stage 2: リプライでURL投稿 (5秒後)
  Utilities.sleep(5000);
  const replyText = `👇 詳細はこちら\n${MY_WEBSITE_URL}`;
  postReplyUrl(replyText, mainTweetId);
  
  // 5. 投稿済みフラグ
  sheet.getRange(rowIndex, 7).setValue("2段階投稿済み");
}
```

**なぜ2段階？**:
- X (Twitter)はリンク付きツイートの露出が低下する
- テキストのみ投稿 → リプライでURL = 高エンゲージメント

**OAuth 1.0a 認証**:
```typescript
function postMainText(text) {
  const url = "https://api.twitter.com/2/tweets";
  
  // OAuth署名生成
  const oauthParams = {
    oauth_consumer_key: getConfig('TWITTER_API_KEY'),
    oauth_token: getConfig('TWITTER_ACCESS_TOKEN'),
    oauth_signature_method: "HMAC-SHA1",
    oauth_timestamp: Math.floor(Date.now()/1000).toString(),
    oauth_nonce: generateSecureNonce(), // 暗号学的に安全な乱数
    oauth_version: "1.0"
  };
  
  const signature = createSignature("POST", url, oauthParams, API_SECRET, TOKEN_SECRET);
  oauthParams.oauth_signature = signature;
  
  // Authorizationヘッダー生成
  const authHeader = "OAuth " + Object.keys(oauthParams)
    .map(k => `${encodeURIComponent(k)}="${encodeURIComponent(oauthParams[k])}"`)
    .join(", ");
  
  // API呼び出し
  const response = UrlFetchApp.fetch(url, {
    method: "post",
    headers: { "Authorization": authHeader, "Content-Type": "application/json" },
    payload: JSON.stringify({ "text": text })
  });
  
  return JSON.parse(response.getContentText()).data.id;
}
```

---

### 4.2 フロントエンド (frontend/index.html)

#### 📝 ファイル: `frontend/index.html` (2117行)

**役割**: ユーザーインターフェース（記事表示、検索、フィルタリング）

#### 主要機能の解説

##### 🔹 記事データ取得

```javascript
async function loadNews() {
  // 1. キャッシュチェック（5分間有効）
  const cachedData = getCachedData();
  if (cachedData) {
    allNews = cachedData;
    displayNews();
    return;
  }
  
  // 2. APIから取得（タイムアウト10秒）
  const controller = new AbortController();
  const timeoutId = setTimeout(() => controller.abort(), 10000);
  
  const response = await fetch('/data/news.json', {
    method: 'GET',
    headers: { 'Accept': 'application/json' },
    signal: controller.signal
  });
  
  clearTimeout(timeoutId);
  
  // 3. データ検証
  const data = await response.json();
  if (!Array.isArray(data) || data.length === 0) {
    throw new Error('Invalid data format');
  }
  
  // 4. XSS対策（エスケープ処理）
  allNews = data.map(item => ({
    date: escapeHtml(item.date || ''),
    title: escapeHtml(item.title || ''),
    url: isValidUrl(item.url) ? item.url : '#',
    summary: escapeHtml(item.summary || ''),
    content: sanitizeHTML(item.content || ''),
    leakScore: Math.min(100, Math.max(0, parseInt(item.leakScore || 75))),
    category: item.category || detectCategory(item.title, item.summary)
  }));
  
  // 5. キャッシュに保存（チェックサム付き）
  setCachedData(allNews);
  displayNews();
}
```

**キャッシュ戦略**:
- `localStorage`に5分間保存
- チェックサム検証で改ざん検出
- キャッシュヒット率: 約80%（サーバー負荷軽減）

---

##### 🔹 カテゴリ自動判定

```javascript
function detectCategory(title, summary) {
  const text = `${title} ${summary}`.toLowerCase();
  
  // GPUパターン（正規表現で単語境界チェック）
  const gpuPatterns = [
    /\brtx\s*\d{4}\b/i,    // "RTX 5090"
    /\bgtx\s*\d{4}\b/i,    // "GTX 1660"
    /\bradeon\b/i,          // "Radeon"
    /\brx\s+\d{4}\b/i,     // "RX 7900"
    /\bgpu\b/i              // "GPU"
  ];
  
  // CPUパターン
  const cpuPatterns = [
    /\bryzen\s+[3579]\b/i,  // "Ryzen 7"
    /\bcore\s+i[3579]\b/i,  // "Core i9"
    /\bsnapdragon\b/i,      // "Snapdragon"
    /\bcpu\b/i              // "CPU"
  ];
  
  // マッチング（優先度順）
  if (gpuPatterns.some(p => p.test(text))) return 'GPU';
  if (cpuPatterns.some(p => p.test(text))) return 'CPU';
  // ... (他のカテゴリ)
  
  return 'General';
}
```

**カテゴリ一覧**:
- GPU, CPU, PC Parts, Laptop, Peripherals, Smartphone, Gaming, AI

---

##### 🔹 モーダル表示（記事詳細）

```javascript
function openModal(news) {
  const modal = document.getElementById('article-modal');
  
  // 多重表示防止
  if (modal.classList.contains('active')) return;
  
  // タイトル設定（言語切り替え対応）
  const displayTitle = currentLang === 'en' 
    ? (news.title_en || news.title) 
    : news.title;
  document.getElementById('modal-title').textContent = displayTitle;
  
  // 信頼度スコア表示
  const leakScore = news.leakScore || 75;
  document.getElementById('modal-score-value').textContent = `${leakScore}%`;
  
  // コンテンツ表示（XSS対策済み）
  const contentHTML = currentLang === 'en' 
    ? (news.content_en || news.content) 
    : news.content;
  document.getElementById('modal-content').innerHTML = contentHTML;
  
  // Utterancesコメント欄を読み込み
  loadUtterances(news.url);
  
  // モーダル表示
  modal.classList.add('active');
  document.body.classList.add('modal-open'); // スクロール禁止
}
```

**アニメーション**:
- 0.3秒のフェードイン
- backdrop-filter: blur(12px) で背景ぼかし
- ESCキーで閉じる

---

### 4.3 GitHub Actions (CI/CD)

#### 📝 ファイル: `.github/workflows/update-news.yml`

**役割**: Google DriveからJSONを自動取得してGitHubに反映

**実行タイミング**:
- 1時間ごと（Cron: `0 * * * *`）
- 手動実行も可能（`workflow_dispatch`）

**処理フロー**:
```yaml
steps:
  # 1. リポジトリをチェックアウト
  - name: Checkout repository
    uses: actions/checkout@v4
  
  # 2. Google DriveからJSON取得
  - name: Download news.json
    run: |
      curl -L -o frontend/data/news.json \
        "https://docs.google.com/uc?export=download&id=17jEZ9eEBdM-nWDErTfjG1rVgW96xI4Nv"
  
  # 3. 🛡️ JSON整合性検証（Fail Fast：壊れたデータをコミットしない）
  - name: Validate JSON integrity
    run: |
      if jq empty frontend/data/news.json > /dev/null 2>&1; then
        echo "✅ JSON is valid."
      else
        echo "❌ Invalid JSON (Google Drive HTML/403等)"
        head -50 frontend/data/news.json
        exit 1
      fi
  
  # 4. 変更チェック
  - name: Check for changes
    id: check_changes
    run: |
      git diff --exit-code frontend/data/news.json || echo "changed=true" >> $GITHUB_OUTPUT
  
  # 5. 変更があればコミット
  - name: Commit and push
    if: steps.check_changes.outputs.changed == 'true'
    run: |
      git add frontend/data/news.json
      git commit -m "🤖 Auto-update: $ARTICLE_COUNT articles"
      git push
  
  # 6. 失敗時にIssue作成
  - name: Create Issue on Failure
    if: failure()
    uses: actions/github-script@v7
    ...
```

**なぜ1時間ごと？**:
- GASは1時間ごとにトリガー実行
- GitHubは1時間ごとにチェック
- 最大1時間の遅延で確実に反映

**GAS Web App URL方式**:
- `GAS_WEB_APP_URL?action=news` でGASが直接スプレッドシートのデータをJSONで返す
- Google Driveの大容量ファイル（2.9MB超）ではHTMLウイルス警告が返るためDrive直接DLは不安定
- ダウンロード後に先頭文字が `[` か `{` かチェック → HTMLならば `exit 1`

**GitHub Secrets**:
| Secret名 | 内容 |
|---|---|
| `GAS_WEB_APP_URL` | GAS Web AppのデプロイURL |

**push競合対策**: コミット直前に `git pull --rebase origin main` を実行し、並行コミットとの競合を防止。

**JSON検証の意義**: `jq empty` で検証し、失敗時はコミットせず `exit 1` で止める（Fail Fast）。

---

### 4.4 Vercel (デプロイ・CDN)

#### 📝 ファイル: `vercel.json`

**役割**: セキュリティヘッダー、CORS設定、キャッシュ戦略

**主要設定**:
```json
{
  "outputDirectory": "frontend",
  "cleanUrls": true,
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        { "key": "X-Content-Type-Options", "value": "nosniff" },
        { "key": "X-Frame-Options", "value": "SAMEORIGIN" },
        { "key": "Strict-Transport-Security", "value": "max-age=31536000" }
      ]
    },
    {
      "source": "/data/(.*)",
      "headers": [
        { "key": "Access-Control-Allow-Origin", "value": "https://gadget-hunter-xi.vercel.app" },
        { "key": "Cache-Control", "value": "public, max-age=300" }
      ]
    }
  ]
}
```

**デプロイフロー**:
1. GitHubにプッシュ
2. Vercel Webhookが発火
3. **ビルドコマンド**: `npm install && npm run build`（`scripts/generate-articles.js` で記事ごとのOGP用HTMLを `frontend/articles/{slug}/index.html` に生成）
4. 自動ビルド（約30秒）
5. グローバルCDNに配信（東京、ニューヨーク、ロンドンなど）
6. HTTPSで即座にアクセス可能

**OGP（SNSカード）**: 各記事URL（例: `/articles/rtx-5070-laptop-power-in-a-tiny-package-...`）はビルド時に静的HTMLが生成され、X/Facebookでリッチカードが表示される。デフォルト画像は `frontend/ogp-default.svg`。

---

### 4.5 データ・運用（クリーンアップ・修復）

| 機能 | 説明 | 参照 |
|------|------|------|
| **古い記事の削除** | `cleanupAndSave(sheet)`: 新しい順に最大300件をキープし、30日超または超過分を削除。**注意: 旧バージョンはループ方向のバグで最新記事が削除されていたが2026-03-02に修正済み**。`manualCleanupOldArticles(days, maxRows)` で手動実行可能。 | `CLEANUP_GUIDE.md` |
| **失敗記事の修復** | `retryFailedArticles(maxRows)`: 直近N行の【翻訳失敗】や英語未入力記事をGeminiで再生成。`repairAllFailedArticles()` で全行チェック可能。 | `REPAIR_FAILED_ARTICLES.md` |
| **移行の合図** | `news.json` が重い・GitHub Diffが辛い・GASメモリ制限に当たり始めたら、DB（Supabase等）や月次アーカイブ（`news_archive_YYYYMM.json`）の検討。コード内コメント参照。 | `SENIOR_SE_REVIEW_RESPONSE.md` |
| **X API制限時** | トリガー停止・72時間冷却・`humanLikeSleep` による人間らしい待機時間。 | `INCIDENT_RESPONSE_X_BAN.md` |

---

## 5. セキュリティ対策

### 🔒 実装済みセキュリティ機能

| 脆弱性 | 対策内容 | 実装箇所 |
|--------|----------|----------|
| **XSS攻撃** | HTMLエスケープ関数 | `frontend/index.html` (1174行) |
| **CSRF攻撃** | CSP設定 | `frontend/index.html` (8-18行) |
| **SQLインジェクション** | N/A（DB不使用） | - |
| **API Key漏洩** | 環境変数化 + ログマスキング | `backend/Code.ts` (56-62行) |
| **DoS攻撃** | レート制限 + タイムアウト | `backend/Code.ts` (303行), `frontend/index.html` (1501行) |
| **情報漏洩** | エラーログのサニタイズ | `backend/Code.ts` (217-232行) |
| **OAuth攻撃** | 暗号学的に安全なnonce | `backend/Code.ts` (100-104行) |
| **キャッシュ改ざん** | チェックサム検証 | `frontend/index.html` (1224-1234行) |

### 🛡️ セキュリティベストプラクティス

#### 1. XSS対策（Cross-Site Scripting）

**脅威**: 悪意あるスクリプトをページに埋め込まれる

**対策**:
```javascript
// ✅ 安全なHTMLエスケープ
function escapeHtml(unsafe) {
  const div = document.createElement('div');
  div.textContent = unsafe; // textContentは自動エスケープ
  return div.innerHTML;
}

// ❌ 危険な例（絶対にやらない）
element.innerHTML = userInput; // XSS脆弱性！
```

#### 2. CSP（Content Security Policy）

**脅威**: 外部からの不正スクリプト実行

**対策**:
```html
<meta http-equiv="Content-Security-Policy" content="
  default-src 'self';
  script-src 'self' 'unsafe-inline' https://utteranc.es https://cdn.jsdelivr.net;
  style-src 'self' 'unsafe-inline' https://fonts.googleapis.com;
  img-src 'self' data: https:;
  connect-src 'self' https://api.exchangerate-api.com;
">
```

**解説**:
- `default-src 'self'`: 基本は同一オリジンのみ許可
- `script-src`: JavaScriptは指定ドメインのみ
- `img-src https:`: 画像はHTTPSのみ

#### 3. OAuth認証（X API）

**脅威**: アクセストークンの盗用

**対策**:
```typescript
// ✅ 暗号学的に安全なnonce生成
function generateSecureNonce() {
  const bytes = Utilities.getUuid() + Date.now() + Math.random();
  return Utilities.base64Encode(
    Utilities.computeDigest(Utilities.DigestAlgorithm.SHA_256, bytes)
  ).substring(0, 32);
}

// ❌ 危険な例
const nonce = Math.random().toString(); // 予測可能！
```

#### 4. エラーログのサニタイズ

**脅威**: API Keyがエラーログに記録される

**対策**:
```typescript
function logError(source, errorType, error, context = '') {
  let errorMessage = error.toString();
  
  // ✅ 機密情報を[REDACTED]に置換
  const sensitivePatterns = [
    /api[_-]?key[=:]\s*[\w-]+/gi,
    /token[=:]\s*[\w-]+/gi,
    /secret[=:]\s*[\w-]+/gi,
    /password[=:]\s*[\w-]+/gi
  ];
  
  sensitivePatterns.forEach(pattern => {
    errorMessage = errorMessage.replace(pattern, '[REDACTED]');
  });
  
  console.error(errorMessage);
}
```

---

## 6. コード詳細解説

### 6.1 重複検知アルゴリズム

#### 🎯 目的
同じニュースが複数のサイトで報道された場合、重複を検出して情報を統合

#### 🔍 Jaccard係数による類似度計算

**数式**:
```
J(A, B) = |A ∩ B| / |A ∪ B|

A: 記事1の単語集合
B: 記事2の単語集合
∩: 積集合（両方に含まれる単語）
∪: 和集合（どちらかに含まれる単語）
```

**実装例**:
```typescript
// タイトル1: "RTX 5090 Leaked Specifications"
// タイトル2: "NVIDIA RTX 5090 Specs Leak"

// 1. 正規化
// → ["rtx", "5090", "leaked", "specifications"]
// → ["nvidia", "rtx", "5090", "specs", "leak"]

// 2. 単語集合
const words1 = new Set(["rtx", "5090", "leaked", "specifications"]);
const words2 = new Set(["nvidia", "rtx", "5090", "specs", "leak"]);

// 3. 積集合 ∩
const intersection = ["rtx", "5090"]; // 2個

// 4. 和集合 ∪
const union = ["rtx", "5090", "leaked", "specifications", "nvidia", "specs", "leak"]; // 7個

// 5. Jaccard係数
const similarity = 2 / 7 = 0.286 (28.6%)
```

**閾値設定**:
- **70%以上**: 重複とみなして統合
- **50-69%**: 類似記事として別保存
- **50%未満**: 完全に別記事

**統合処理**:
```typescript
if (similarity >= 0.7) {
  // 既存記事を更新
  const updatedScore = Math.min(100, existingScore + 15); // +15ポイント
  const updatedContent = mergeContents(existing, newArticle);
  
  sheet.getRange(rowIndex, 5).setValue(updatedContent);
  sheet.getRange(rowIndex, 6).setValue(updatedScore);
  
  console.log(`✅ 統合完了: ${existingScore} → ${updatedScore}`);
}
```

---

### 6.2 リーク信頼度スコア計算

#### 🎯 目的
ニュースの信頼性を0-100%で定量評価

#### 📊 スコア計算式

```typescript
function calculateLeakScore(article) {
  let score = 40; // ベーススコア
  
  // 1. ソース信頼度（0-30点）
  score += getSourceScore(article.url);
  
  // 2. 証拠の質（0-25点）
  score += getEvidenceScore(article.title, article.summary);
  
  // 3. 具体性（0-20点）
  score += getSpecificityScore(article.title, article.summary);
  
  // 4. 確実性（-10～+15点）
  score += getCertaintyScore(article.title, article.summary);
  
  // 5. 時間性（0-10点）
  score += getTimelinessScore(article.title, article.summary);
  
  // 6. 範囲制限（0-100）
  return Math.max(0, Math.min(100, Math.round(score)));
}
```

#### 詳細ロジック

##### 1️⃣ ソース信頼度（0-30点）

```typescript
function getSourceScore(url) {
  const domain = extractDomain(url);
  const scores = {
    'apple.com': 30,        // 公式ソース（最高信頼）
    'samsung.com': 30,
    'nvidia.com': 30,
    'techpowerup.com': 20,  // 専門メディア
    'wccftech.com': 18,
    'nitter.net': 15,       // Twitterリーク
    'xcancel.com': 15
  };
  return scores[domain] || 10; // デフォルト: 10点
}
```

**解説**:
- 公式サイト > 専門メディア > Twitter > その他

##### 2️⃣ 証拠の質（0-25点）

```typescript
function getEvidenceScore(text) {
  let score = 0;
  
  // 公式発表・ベンチマーク画像あり
  if (/official|発表|benchmark|流出/i.test(text)) {
    score += 25;
  }
  // 信頼できるリーカー
  else if (/kopite7kimi|momomo_us|HXL/i.test(text)) {
    score += 20;
  }
  // 一般的なリーク
  else {
    score += 10;
  }
  
  return score;
}
```

##### 3️⃣ 具体性（0-20点）

```typescript
function getSpecificityScore(text) {
  let score = 0;
  
  // 価格情報あり
  if (/\$\d+|¥\d+/.test(text)) score += 5;
  
  // スペック詳細あり
  if (/\d+GB|GHz|MHz|W|CUDA/.test(text)) score += 5;
  
  // モデル番号あり
  if (/RTX\s*\d{4}|RX\s*\d{4}|Ryzen\s*\d/.test(text)) score += 5;
  
  // 発売日情報あり
  if (/202\d年|Jan|Feb|Mar/.test(text)) score += 5;
  
  return Math.min(20, score);
}
```

##### 4️⃣ 確実性（-10～+15点）

```typescript
function getCertaintyScore(title, summary) {
  const text = (title + ' ' + summary).toLowerCase();
  
  // 確定情報
  if (/confirmed|official|公式/.test(text)) return 15;
  
  // 信頼できるリーク
  if (/leak|rumor|噂/.test(text)) return 5;
  
  // 推測・憶測
  if (/speculation|guess|予想/.test(text)) return -5;
  
  // 未確認情報
  if (/unconfirmed/.test(text)) return -10;
  
  return 0;
}
```

##### 5️⃣ 時間性（0-10点）

```typescript
function getTimelinessScore(text) {
  // 近日発売
  if (/soon|coming|発売間近/.test(text)) return 10;
  
  // 今年中
  if (/202\d/.test(text)) return 5;
  
  return 0;
}
```

#### スコア例

| 記事内容 | スコア | 理由 |
|---------|--------|------|
| NVIDIA公式発表「RTX 5090 $1999」 | **95%** | 公式(30) + 発表(25) + 価格(5) + モデル(5) + 確定(15) + 時間(5) = 95 |
| kopite7kimi「RTX 5090 21,760コア」 | **80%** | Twitter(15) + リーカー(20) + スペック(10) + モデル(5) + リーク(5) = 75 + 信頼補正(+5) |
| 匿名掲示板「RTX 5090は来年？」 | **45%** | 不明(10) + 推測(-5) + モデル(5) = 40 + 時間(5) |

---

### 6.3 AI記事生成（プロンプトエンジニアリング）

#### 🎯 目的
英語ニュースを日本語の「19歳自作erペルソナ」で記事化

#### 📝 プロンプト設計の3原則

##### 1️⃣ ペルソナ定義（Who）

```markdown
# あなたは誰か
匿名の19歳・大学2年生（情報工学専攻）

## プロフィール
- PC: Ryzen 7 7800X3D + Radeon RX 9070 XT
- スマホ: Poco X7 Pro、RedMagic Astra
- 好きなゲーム: CoD Warzone、Minecraft、ARK
- 哲学: Performance per Yen（円パフォーマンス） > ブランド信仰

## 口癖
✅ 「正直」「個人的には」「〜ですね」
❌ 「〜ですわ」「めっちゃヤバい！！！」（AI臭い）
```

**効果**: AIが一貫したキャラクターで記事を書く

##### 2️⃣ 文体ルール（How）

```markdown
## 人間っぽさ = 3つの要素

### 1. 主観と偏愛
❌ AI: 「この製品はコスパが高いです。」
✅ 人間: 「この性能でこの価格？正気かよ（褒め言葉）。」

### 2. 比較という文脈
❌ AI: 「前モデルより20%向上しました。」
✅ 人間: 「僕の7800X3Dと比べても、これは...（実体験）」

### 3. 毒とスラング
❌ AI: 「初期ロットには不具合の可能性があります。」
✅ 人間: 「どうせいつもの人柱案件だろ？様子見安定。」
```

**効果**: AI特有の「無難な表現」を排除

##### 3️⃣ 出力フォーマット（What）

```json
{
  "title_jp": "【リーク】RTX 5090、21,760コア&600W爆熱仕様で2025年1月発表か",
  "title_en": "Leak: RTX 5090 with 21,760 Cores & 600W TDP Coming Jan 2025",
  "summary_points": [
    "21,760 CUDAコア搭載、GB202フルダイ構成（RTX 4090比+33%）",
    "TDP 600W、12VHPWRコネクタ2本構成の可能性（電源1000W必須レベル）",
    "2025年1月CES発表、2月下旬発売との予測（様子見安定）"
  ],
  "body_text": "<p>信頼性の高いリーカー<strong>kopite7kimi</strong>氏によると...</p>",
  "review_text": "正直、600Wは引きました。電源ユニットをケチって後悔した経験がある身としては..."
}
```

**効果**: 構造化されたJSON出力で後処理が簡単

#### 実例：プロンプト→記事生成

**入力**:
```
タイトル: "AMD Ryzen 9 9950X3D Rumored to Launch in January 2025"
内容: "Leaked slides suggest AMD's next-gen Ryzen 9 9950X3D will feature 3D V-Cache and launch at CES 2025..."
```

**出力**:
```json
{
  "title_jp": "【リーク】Ryzen 9 9950X3D、2025年1月CES発表か—3D V-Cache搭載の最強ゲーミングCPU",
  "review_text": "正直、9950X3Dが来るなら7800X3Dは様子見すればよかった...でも3D V-Cacheの実力は折り紙付きなので、ゲーマーには朗報ですね。ただ価格が10万円超えなら学生には厳しいかも。"
}
```

**ポイント**:
- ✅ 「正直」「様子見」などの口癖
- ✅ 自分のPC（7800X3D）との比較
- ✅ 学生目線の予算感覚（10万円）

---

## 7. デプロイとCI/CD

### 🚀 デプロイフロー全体図

```
開発マシン
  │
  ├─ コード編集
  │  └─ backend/Code.ts, frontend/index.html
  │
  ├─ GASエディタ
  │  └─ Apps Scriptにコピー&ペースト
  │
  ├─ トリガー設定
  │  └─ fetchAndSummarizeToSheet() → 2時間ごと
  │     checkAndTweetNewArticles() → 1時間ごと
  │
  └─ Git Push
     └─ git push origin main

GitHub
  │
  ├─ コードリポジトリ
  │  └─ gadget-hunter/
  │
  ├─ GitHub Actions起動 (1時間ごと)
  │  └─ update-news.yml
  │      ├─ Google DriveからJSON取得
  │      ├─ git commit & push
  │      └─ Vercel Webhookトリガー
  │
  └─ Vercel連携

Vercel
  │
  ├─ 自動ビルド（約30秒）
  │  └─ frontend/ をビルド
  │
  ├─ CDN配信
  │  └─ 世界200+拠点
  │
  └─ デプロイ完了
     └─ https://gadget-hunter-xi.vercel.app/

ユーザー
  └─ 最新ニュースを閲覧
```

### 📝 デプロイ手順（実務）

#### ステップ1: コード修正

```bash
# 1. ブランチ作成
git checkout -b feature/new-feature

# 2. コード編集
code backend/Code.ts

# 3. テスト実行（GASエディタで）
# → fetchAndSummarizeToSheet() を手動実行
# → ログ確認

# 4. コミット
git add .
git commit -m "feat: 新機能追加"
git push origin feature/new-feature
```

#### ステップ2: プルリクエスト

```bash
# GitHub上でPR作成
# → レビュー依頼
# → CIチェック（GitHub Actions）
# → マージ承認
```

#### ステップ3: 本番デプロイ

```bash
# mainブランチにマージ
git checkout main
git pull origin main

# GASに反映（手動）
# → Apps Scriptエディタでコピー
# → デプロイ（新バージョン）

# フロントエンドは自動デプロイ
# → Vercelが自動検知してデプロイ
```

---

## 8. トラブルシューティング

### 🐛 よくある問題と解決策

#### 問題1: 「news.json が403エラーで取得できない」

**原因**: Google Driveの共有設定が不適切

**解決策**:
```bash
1. Google Driveでnews.jsonを右クリック
2. 「共有」→「リンクを知っている全員」に変更
3. ファイルIDを確認: 1Q0CjYkMIJOKPZBW6OLj3sP_pcmmm4fdx
4. GitHub Actionsを再実行
```

---

#### 問題2: 「Gemini APIがRate Limit Exceeded」

**原因**: 1分間に15回以上API呼び出し

**解決策**:
```typescript
// backend/Code.ts の303行目
const MAX_API_CALLS = 30; // ← 15に変更

// または、sleep時間を延長
Utilities.sleep(5000); // 5秒 → 10秒
```

---

#### 問題3: 「X投稿が失敗する」

**原因**: OAuth署名エラー、API制限

**デバッグ方法**:
```typescript
// backend/Code.ts の823行目
function postMainText(text) {
  try {
    const response = UrlFetchApp.fetch(url, { ... });
    console.log(response.getContentText()); // ← 追加
    return JSON.parse(response.getContentText()).data.id;
  } catch (e) {
    console.error(`❌ Twitter API Error: ${e.message}`);
    console.error(`Response: ${e.response}`); // ← 追加
    throw e;
  }
}
```

**確認項目**:
- [ ] TWITTER_API_KEY が正しいか
- [ ] TWITTER_ACCESS_TOKEN が有効か
- [ ] API利用制限（1日50投稿）を超えていないか

---

#### 問題4: 「フロントエンドで記事が表示されない」

**原因**: JSONパースエラー、XSS対策の過剰フィルタリング

**デバッグ方法**:
```javascript
// ブラウザのDevToolsコンソールで
fetch('/data/news.json')
  .then(r => r.json())
  .then(data => console.log(data))
  .catch(e => console.error(e));
```

**確認項目**:
- [ ] `/data/news.json` が正しいパスか
- [ ] JSONの構文が正しいか（カンマ・括弧）
- [ ] ブラウザキャッシュをクリアしたか

---

#### 問題5: 「GitHub Actionsが失敗する」

**原因**: Google DriveのファイルIDが変更された

**解決策**:
```yaml
# .github/workflows/update-news.yml のダウンロードステップ
curl -L -o frontend/data/news.json \
  "https://docs.google.com/uc?export=download&id=【新しいファイルID】"
```

**ファイルIDの確認方法**:
```
Google DriveでURL確認
例: https://drive.google.com/file/d/17jEZ9eEBdM-nWDErTfjG1rVgW96xI4Nv/view
                                     ↑ この部分がファイルID
```

---

#### 問題6: 「Validate JSON integrity で失敗する」

**原因**: Google Driveが大容量時にHTML（ウイルススキャン警告）や403を返し、`news.json` が実際にはJSONでない

**解決策**:
1. GAS側で `news.json` を「上書き更新」しているか確認（File ID固定化）。`backend/Code.ts` の `saveJsonToDrive` を参照。
2. 手動で正しいJSONをダウンロードし、`frontend/data/news.json` に置いてコミットしてから再実行。
3. ワークフロー実行ログの「First 50 lines」で中身を確認。HTMLが返っていればDriveの制限が原因。

---

## 9. 用語集

### 技術用語解説

| 用語 | 読み方 | 意味 |
|------|--------|------|
| **RSS** | アールエスエス | Really Simple Syndication（Webサイトの更新情報を配信する規格） |
| **API** | エーピーアイ | Application Programming Interface（プログラム間の通信手段） |
| **JSON** | ジェイソン | JavaScript Object Notation（データ交換フォーマット） |
| **XSS** | クロスサイトスクリプティング | Cross-Site Scripting（Webサイトに悪意あるスクリプトを埋め込む攻撃） |
| **CSRF** | クロスサイトリクエストフォージェリ | Cross-Site Request Forgery（偽のリクエストで不正操作を実行させる攻撃） |
| **CSP** | コンテンツセキュリティポリシー | Content Security Policy（XSS対策のセキュリティ機能） |
| **CDN** | シーディーエヌ | Content Delivery Network（世界中のサーバーにコンテンツを配信する仕組み） |
| **OAuth** | オーオース | Open Authorization（第三者アプリケーションに権限を委譲する認証方式） |
| **Webhook** | ウェブフック | Webサービス間でイベント通知を送る仕組み |
| **Nonce** | ノンス | Number used once（1回だけ使用される乱数、リプレイ攻撃対策） |
| **Jaccard係数** | ジャッカードけいすう | 2つの集合の類似度を測る指標（0～1の範囲） |

### プロジェクト固有用語

| 用語 | 意味 |
|------|------|
| **リークスコア** | ニュースの信頼性を0-100%で表した指標 |
| **2段階投稿** | Xでテキストのみ投稿→リプライでURL投稿する手法 |
| **重複検知** | 同じニュースを複数ソースから検出する機能 |
| **ペルソナ** | AIに与える「19歳自作er学生」という人格設定 |
| **コバンザメ戦略** | 他人のツイートに引用RTで相乗りする手法 |
| **ノイズフィルター** | 無関係な記事を除外するキーワードフィルター |

---

## 🎓 学習の次のステップ

### 初級（現在のプロジェクトを理解する）

- [ ] `backend/Code.ts` を1行ずつ読む
- [ ] GASエディタでコードを実行してログを確認
- [ ] `frontend/index.html` のJavaScriptを理解
- [ ] DevToolsでネットワークタブを確認

### 中級（機能拡張）

- [ ] 新しいRSSフィードを追加
- [ ] カテゴリーを追加（例: Tablet）
- [ ] リークスコアの計算式を改善
- [ ] フロントエンドにダークモード切り替えを追加

### 上級（新規機能開発）

- [ ] ユーザー登録・ログイン機能
- [ ] お気に入り記事の保存
- [ ] Blueskyへの自動投稿
- [ ] モバイルアプリ化（React Native）

---

## 📚 参考資料

### 本プロジェクト内ドキュメント（状況に応じて更新）

| ファイル | 内容 |
|----------|------|
| `CLEANUP_GUIDE.md` | スプレッドシート・古い記事の削除（自動・手動） |
| `REPAIR_FAILED_ARTICLES.md` | 過去の失敗記事（AI制限等）の修復手順 |
| `OGP_IMPLEMENTATION_GUIDE.md` | OGP・ビルド時静的生成の実装とテスト |
| `INCIDENT_RESPONSE_X_BAN.md` | X API制限時のインシデント対応・再発防止 |
| `SENIOR_SE_REVIEW_RESPONSE.md` | シニアSEレビューへの回答・条件付き承認 |

### 公式ドキュメント

- [Google Apps Script公式](https://developers.google.com/apps-script)
- [Gemini API公式](https://ai.google.dev/docs)
- [X API v2公式](https://developer.twitter.com/en/docs/twitter-api)
- [Vercel公式](https://vercel.com/docs)
- [MDN Web Docs](https://developer.mozilla.org/)

### セキュリティ

- [OWASP Top 10](https://owasp.org/www-project-top-ten/)
- [CSP Reference](https://content-security-policy.com/)

---

## ✉️ お問い合わせ

質問・バグ報告は GitHubの[Issues](https://github.com/chinodevcontact-ops/gadget-hunter/issues)へ

---

**最終更新**: 2026年1月19日  
**作成者**: GADGET HUNTER開発チーム  
**ライセンス**: MIT License
