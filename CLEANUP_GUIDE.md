# 🗑️ スプレッドシート クリーンアップガイド

## 📋 概要

スプレッドシートに古い記事データがたまりすぎるのを防ぐため、**日付ベース**と**行数制限**の両方で自動削除する機能を実装しました。

---

## ⚙️ 設定

`backend/Code.ts` の設定定数で変更可能：

```typescript
const CLEANUP_DAYS_TO_KEEP = 30; // 30日以上古い記事を削除
const CLEANUP_MAX_ROWS = 300;    // 最大300行まで保持（ヘッダー除く）
```

---

## 🔄 自動削除

**通常の実行時**（`main()`関数実行時）に自動的にクリーンアップが実行されます。

**削除条件**:
- ✅ 30日以上古い記事（日付列で判定）
- ✅ または、300行を超えた場合（古いものから削除）

**ログ出力例**:
```
🗑️ 古い記事 45 件を削除しました（30日以上前、または300行超過）
```

---

## 🛠️ 手動削除（即座に実行）

GASエディタから直接実行できます。

### 手順

1. **GASエディタを開く**: https://script.google.com/
2. **関数を選択**: `manualCleanupOldArticles`
3. **実行**ボタンをクリック

### カスタマイズ例

```javascript
// デフォルト（30日以上前、最大300行）
manualCleanupOldArticles();

// より積極的（7日以上前、最大100行）
manualCleanupOldArticles(7, 100);

// より保守的（60日以上前、最大500行）
manualCleanupOldArticles(60, 500);
```

---

## 📊 削除ロジック

### 1. 日付ベース削除
```
現在日時 - 保持日数 = カットオフ日時
↓
カットオフ日時より古い記事を削除
```

### 2. 行数制限削除
```
現在の行数 > 最大保持行数 + 1（ヘッダー）
↓
古い記事から順に削除（最新の記事を優先保持）
```

### 3. 両方の条件をチェック
- **日付が古い** OR **行数超過** → 削除対象

---

## ⚠️ 注意事項

1. **削除は不可逆**: 一度削除したデータは復元できません
2. **JSONファイルは自動更新**: スプレッドシート削除後、`news.json`も自動的に更新されます
3. **ヘッダー行は保護**: 1行目のヘッダーは削除されません
4. **日付が無効な行**: 日付が正しくパースできない行はスキップされます

---

## 🔍 トラブルシューティング

### 削除されない場合

1. **日付列の形式を確認**:
   - 正しい形式: `2026/01/20` または `Date`オブジェクト
   - 無効な形式: 空欄、文字列（日付として解釈できない）

2. **手動で実行**:
   ```javascript
   manualCleanupOldArticles(0, 100); // 即座に100行以下に削減
   ```

3. **ログを確認**:
   - GASエディタの「実行」タブでログを確認
   - `🗑️ 古い記事 X 件を削除しました` のメッセージを確認

---

## 📈 推奨設定

### 小規模運用（1日1〜5記事）
```typescript
const CLEANUP_DAYS_TO_KEEP = 60;  // 2ヶ月保持
const CLEANUP_MAX_ROWS = 200;      // 200行まで
```

### 中規模運用（1日5〜10記事）
```typescript
const CLEANUP_DAYS_TO_KEEP = 30;  // 1ヶ月保持（デフォルト）
const CLEANUP_MAX_ROWS = 300;     // 300行まで（デフォルト）
```

### 大規模運用（1日10記事以上）
```typescript
const CLEANUP_DAYS_TO_KEEP = 14;  // 2週間保持
const CLEANUP_MAX_ROWS = 200;     // 200行まで
```

---

## 🎯 使用例

### 例1: 即座にクリーンアップしたい
```javascript
// GASエディタで実行
manualCleanupOldArticles(0, 50); // 全ての古い記事を削除、最新50行のみ保持
```

### 例2: 1週間以上古い記事を削除
```javascript
manualCleanupOldArticles(7, 500); // 7日以上前を削除、最大500行保持
```

### 例3: 行数制限のみ（日付は無視）
```javascript
manualCleanupOldArticles(9999, 100); // 実質的に行数制限のみ
```

---

**これで、スプレッドシートが肥大化する心配はありません！** 🎉
